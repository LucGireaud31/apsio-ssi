import { View, StyleSheet } from "react-native"

interface IdentityCardFormProps{
};

export function IdentityCardForm(props:IdentityCardFormProps) {
const {} = props;

return (
<View style={styles.container}></View>
);
}

const styles = StyleSheet.create({
container:{

}
});